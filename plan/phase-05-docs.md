# Phase 5 — Update Documentation

## Goal
Update `docs/strategies.md` to replace all YAML references with JSON format examples and reflect the new strategy definition schema.

## TODOs
- [x] Replace "YAML Strategy Definitions" section header and description in `docs/strategies.md` with "JSON Strategy Definitions"
- [x] Replace the YAML custom strategy example with the equivalent JSON format
- [x] Update the file listing to reflect JSON-only structure (remove `markets/` since those are now inline)
- [x] Remove any reference to `CUSTOM_STRATEGIES.md` if it doesn't exist or is YAML-based

## Notes

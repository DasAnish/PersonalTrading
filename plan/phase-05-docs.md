# Phase 5 — Update Documentation

## Goal
Update `docs/strategies.md` to replace all YAML references with JSON format examples and reflect the new strategy definition schema.

## TODOs
- [ ] Replace "YAML Strategy Definitions" section header and description in `docs/strategies.md` with "JSON Strategy Definitions"
- [ ] Replace the YAML custom strategy example with the equivalent JSON format
- [ ] Update the file listing to reflect JSON-only structure (remove `markets/` since those are now inline)
- [ ] Remove any reference to `CUSTOM_STRATEGIES.md` if it doesn't exist or is YAML-based

## Notes

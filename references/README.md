# references/

Third-party research material and reference implementations kept for study. **Not imported by the project** — external code with its own licenses.

- `Hierarchical-Risk-Parity/` — De Prado's HRP notebook, paper, and sample pricing data
- `pypbo/` — reference implementation of Probability of Backtest Overfitting (see its own `LICENSE`)

Our own implementations live in `analytics/` and `strategies/`; these are here for provenance and cross-checking.

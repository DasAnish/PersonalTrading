# Minimum Variance Portfolio Optimization
# Finds the lowest-volatility portfolio on the efficient frontier
type: allocation
class: MinimumVarianceStrategy
market: uk_etfs
description: |
  Minimum Variance optimization finds the portfolio with the lowest
  possible volatility using quadratic optimization (scipy SLSQP).
  Long-only constraint with weights summing to 1.

  Best for: Risk-averse investors wanting minimal portfolio volatility

parameters: {}

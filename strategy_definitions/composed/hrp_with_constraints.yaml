# Composed Strategy: HRP + Constraints (10%-30%)
# Combines HRP optimization with weight constraints
type: composed
description: |
  HRP allocation with strict weight constraints.

  Composition:
  1. Market: UK ETFs (VUSA, SSLN, SGLN, IWRD)
  2. Allocation: HRP Ward (hierarchical clustering)
  3. Overlay: Constraints (10% min, 30% max per position)

  Characteristics:
  - Diversified HRP weights
  - Enforces balanced position sizing
  - No concentration above 30%
  - Minimum 10% per active position

  Best for: Conservative, well-balanced portfolios

layers:
  - constraints_10_30
